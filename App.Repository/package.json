{
  "name": "app.repository",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "build:docker": "bun run build.ts --docker",
    "build:docker:tag": "bun run build.ts --docker --tag"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
