# Backend Domain

This project (`Backend.API.Domain`) represents the heart of the business domain.

## Responsibilities

- **Entities**: Database entities (e.g., `User`, `Project`).
- **Enums**: Domain-specific enumerations.
- **Base Types**: Common base classes like `BaseEntity`.

This project has **zero dependencies** on other projects in the solution, ensuring the domain model remains pure.
