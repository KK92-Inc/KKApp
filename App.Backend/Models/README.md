# Backend Models

This project (`Backend.API.Models`) defines the data structures used for communication.

## Responsibilities

- **Request DTOs**: Objects defining data expected in API requests.
- **Response DTOs**: Objects defining data returned by the API.

Separating models prevents exposing internal Domain entities directly to the API consumers.
